import { MailPlus } from 'lucide-react';
import type { LucideProps } from 'lucide-react';

export default function AppLogoIcon(props: LucideProps) {
    return <MailPlus {...props} strokeWidth={2.25} />;
}
